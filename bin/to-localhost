#!/bin/bash

if [ $# = 0 ]; then
    open -a Firefox "http://localhost:8080/manager"
fi
if [ $# = 1 ]; then
    open -a Firefox "http://localhost:8080/$1"
fi
if [ $# -gt 1 ]; then
    echo "too many args"
fi
